# T8.4 — Durable storage and history cleanup

## Objective

Đưa production data ra userData, ghi history atomic và không để output mồ côi.

## Paths

```text
backend/src/config.py
backend/src/main.py
backend/src/services/history_service.py
frontend/electron/main.ts
backend/tests/test_history_service.py
```

## Implementation Notes

- `DOCUMARK_DATA_DIR` override, project `data/` chỉ là dev fallback.
- Atomic replace history file.
- Append trả evicted job IDs để cleanup outputs.
- `record_history=false` chỉ trả result trong memory/job registry.

## Verification

- History corruption/eviction tests pass.
- Region conversion không tạo persistent output.
