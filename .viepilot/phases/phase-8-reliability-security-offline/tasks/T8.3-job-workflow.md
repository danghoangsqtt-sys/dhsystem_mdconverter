# T8.3 — Job workflow and real progress

## Objective

Thay progress timer bằng lifecycle thực từ backend và thêm cancel semantics trung thực.

## Paths

```text
backend/src/main.py
backend/src/services/job_service.py
frontend/src/services/api.ts
frontend/src/App.tsx
frontend/src/types.ts
frontend/src/components/ProcessingStatus.tsx
backend/tests/test_job_service.py
```

## Implementation Notes

- POST trả 202 sau upload; polling status; result endpoint riêng.
- Queue giới hạn một conversion.
- Cancel queued ngay; running chuyển `cancelling`, discard result khi native call kết thúc.
- UI không hiển thị phần trăm/stage giả.

## Verification

- Hai job xếp hàng đúng thứ tự.
- Cancel state không báo thành công trước khi worker kết thúc.
- Frontend build và typecheck pass.
